# UI-002：雷达目标类别与时效样式验证报告

## 1. 结论

- 结论：**本地门禁通过，可提交 PR 评审**
- Issue：`#8`
- 候选提交：`2a5b98343eea3396fd4a30cb73ff526f59191128`
- 分支：`ui/UI-002-radar-target-style`
- Run ID：`UI002_validation_20260906_163920`
- 验证日期：2026-09-06

本次只调整 `DMX_test` 的雷达目标绘制。正式 `DMX` 保持原有目标样式；目标保留时长、事件融合、YOLO 阈值、设备控制、端口和数据路径均未修改。

## 2. 实现结果

- `drone` 使用红色圆点；`bird`、民航客机和其他/未知类别使用绿色圆点。
- 实时命中目标使用实心圆点；新命中阶段增加短时脉冲外环。
- 暂时未命中但仍在保留期内的目标使用低透明度空心圆和虚线外环。
- 已确认目标增加稳定细外环；选中目标增加青色焦点环，不覆盖类别颜色。
- 雷达左上角增加红/绿类别和实心/空心时效图例。
- 紧凑雷达与完整雷达复用 `TargetRadarWidget`，因此样式映射一致。
- 超过保留期的目标继续由既有逻辑隐藏，不留下可点击标记。

## 3. 自动测试

| 门禁 | 结果 |
| --- | --- |
| Python `pytest` | 37 passed |
| AppConfig QtTest | 6 passed，0 failed |
| PanoramaCache QtTest | 7 passed，0 failed |
| ManualNegativeStore QtTest | 7 passed，0 failed |
| CompactTargetRadarPanel QtTest | 5 passed，0 failed |
| 新增 TargetRadarWidget QtTest | 7 passed，0 failed |
| JSON、Shell、Python 编译检查 | 通过 |
| Git 空白错误检查 | 通过 |

新增 QtTest 覆盖无人机/飞鸟/其他类别颜色、选中飞鸟不变红、历史目标空心样式、过期目标隐藏和截图生成。

## 4. 双程序构建

| 产物 | 结果 | SHA-256 |
| --- | --- | --- |
| DMX | 成功 | `3bbd56e47085efa7efc070234910c8e06fc4d721785daa2a045a219e5da68051` |
| DMX_test | 成功 | `464f98b6d829c4bfd8cd6b94de7a3cafa5c7115e9c6ea7fee45eab8dbf3c6f6c` |

两项均在独立空构建目录内完成。构建仅出现工程既有的 Qt 弃用和未使用函数警告，没有编译失败。

## 5. 截图检查

| 项目 | 结果 |
| --- | --- |
| 截图尺寸 | 640 x 420 |
| 无人机红色、其他类别绿色 | 通过 |
| 实时实心、历史空心虚线 | 通过 |
| 选中态不覆盖类别色 | 通过 |
| 图例、方位文字、目标标记重叠 | 未发现 |
| 截图 SHA-256 | `000aa85fea7c80f601aed6ea9e9db2f30efa37685cc1f56a6049c8f80f2c282d` |

截图证据位于本机忽略目录：

```text
test-results/UI002_validation_20260906_163920/ui/radar_style.png
```

## 6. 配置与验证边界

- `dmx_config.json` 未修改，验证时 SHA-256 为 `ca164c682828210e9c8532abf991009fa15edebf0da6f8eb0e8132b745c8b648`。
- 模型权重未修改，也未参与本次纯绘制单元测试。
- 按 Issue 测试计划，本次不要求固定数据回放或真机控制。
- 截图使用 `DMX_TEST_BUILD` 条件编译路径；正式构建路径由现有紧凑雷达 QtTest 和双程序构建覆盖。
- GitHub Actions 状态需在 PR 创建后确认。

## 7. 回滚

合并后若出现视觉回归，使用 `git revert` 回退 UI-002 的报告提交和候选实现提交，再重新执行快速测试与双程序构建。
