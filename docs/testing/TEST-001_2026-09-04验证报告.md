# TEST-001：统一测试入口与 PR 快速 CI 验证报告

## 1. 结论

- 结论：**本地门禁通过，可推送并创建 PR**
- 源码提交：`135915fc2b565ff4e971088c7dc8a5d431311b78`
- 分支：`feature/TEST-001-unified-test-ci`
- Run ID：`TEST001_validation_20260904_184106`
- 验证日期：2026-09-04

TEST-001 为 DMX 增加统一的快速测试、双程序构建和本地回放冒烟入口，并在 Pull Request 与 `main` 推送时运行 GitHub Actions 快速测试和 Linux 构建。它不替代全天回放、算法精度或真机验收。

## 2. 变更范围

- `scripts/test_fast.sh`：JSON/Shell 检查、Python 测试和三组 QtTest。
- `scripts/test_build.sh`：在独立目录完整构建 `DMX` 和 `DMX_test`。
- `scripts/test_replay_smoke.sh`：本地 UDP 回放、离屏启动和关键日志断言。
- `.github/workflows/pr-fast-ci.yml`：PR 快速测试与 Linux 双程序构建。
- `test-results/<Run ID>/<suite>/`：统一元数据、状态、日志和专项产物。

## 3. 本地静态检查

| 检查 | 结果 |
| --- | --- |
| ShellCheck 0.11.0 | 通过 |
| Actionlint 1.7.12 | 通过 |
| Workflow YAML 解析与作业结构检查 | 通过 |
| `git diff --check` | 通过 |
| 从项目目录外执行 `--help` | 通过 |
| 回放日志路径缺失预检 | 通过，明确报错并以退出码 2 终止 |

## 4. 快速测试

| 门禁 | 结果 |
| --- | --- |
| Python `pytest` | 37 passed |
| AppConfig QtTest | 6 passed，0 failed |
| PanoramaCache QtTest | 7 passed，0 failed |
| ManualNegativeStore QtTest | 7 passed，0 failed |
| JSON 与 Shell 语法 | 通过 |
| Python 依赖与 `compileall` | 通过 |

快速测试共覆盖 37 个 Python 测试和 20 个 QtTest 用例。命令从 `/tmp` 目录执行，证明入口不依赖调用者当前目录。

## 5. 双程序构建

| 产物 | 结果 | 警告 | SHA-256 |
| --- | --- | --- | --- |
| DMX | 成功 | 13 | `189ccf84fc279445249ab1da2299a29419764f1716681d090509f70d534d8219` |
| DMX_test | 成功 | 13 | `5a10ff0e2ca74d71e6f4ed35030c8a0f0693a821092bbd9f4ea5235d7cc2b6fc` |

两项均在本次 Run ID 的空构建目录内完成。13 条警告与 OPS-001 基线一致，主要为 Qt 弃用 API 和未使用代码，TEST-001 未修改功能代码。

## 6. 回放冒烟测试

| 指标 | 结果 |
| --- | --- |
| 运行窗口 | 45 s |
| RGB/BW 接收摘要 | 44 次 |
| YOLO 结果 | 20 次 |
| 回放控制应答 | 通过 |
| Direct YOLO worker 就绪 | 通过 |
| 图像读取失败 | 0 |
| 结束后残留进程 | 0 |
| 结束后测试端口占用 | 0 |

本项证明统一脚本能启动 2026-07-23 本地数据回放，驱动 `DMX_test` 接收 RGB/BW 通知并启动 YOLO worker。结果数量只用于链路冒烟，不代表识别精度。

## 7. 证据目录

```text
/mnt/dmx4t/data/dmx_test/validation/TEST001_validation_20260904_184106
```

`fast`、`build` 和 `replay-smoke` 三套 `status.txt` 均记录 `status=passed`、`exit_code=0`。目录还包含 JUnit XML、分步骤日志、二进制哈希、应用日志和回放摘要。

## 8. CI 边界与待确认项

- 云端 CI 只运行快速测试和 Linux 双程序构建，不访问本地模型、共享盘、4T 数据盘、GPU、相机或转台。
- 本地回放仍是合并前门禁，用于覆盖真实数据与 YOLO 启动链路。
- GitHub Actions 结果需在分支推送并创建 PR 后确认；只有两个作业均通过，TEST-001 才满足合并条件。
- 本次未执行全天回放、性能分位数统计、算法精度评估和真机联调。
