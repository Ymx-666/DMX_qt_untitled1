# DOC-001 验证报告

- Issue：`#6 [DOC-001] 固化 DMX 项目开发流程与新会话规则`
- 分支：`docs/DOC-001-persist-development-workflow`
- 候选 Commit：`2d67ce40255129d9898c3be307cecf32a7de3432`
- 最终 Run ID：`DOC001_validation_20260906_112135`
- 验证时间：2026-09-06 11:21，Asia/Shanghai
- 变更类型：项目规则与文档，不涉及程序逻辑、配置、模型或设备

## 改动摘要

1. 新增根目录 `AGENTS.md`，将 DMX 项目的强制开发、测试、合并和发布规则设为新对话自动发现的项目级指令。
2. 新增 `docs/项目管理/DMX每次改动标准流程.md`，提供从 Issue 到 Release、紧急回滚和单人职责划分的完整操作步骤。
3. 更新 `TASK_HANDOFF.md`，把新会话入口指向 `AGENTS.md` 和标准流程。
4. 创建桌面符号链接 `/home/sht/桌面/DMX项目开发流程.md`，目标为仓库内受 Git 管理的流程正文。

## 验证内容

- 对候选 Commit 执行 `git diff-tree --check`，无空白错误。
- 候选 Commit 仅包含 `AGENTS.md`、`TASK_HANDOFF.md` 和标准流程正文三个预期文件。
- 校验两个 Markdown 文件的围栏代码块成对闭合。
- 校验 Issue、开发分支、Run ID、用户合并等关键规则存在。
- 校验交接索引引用 `AGENTS.md`。
- 校验流程引用的交接、测试和工程化策略文件存在且可读。
- 校验桌面入口是有效符号链接，并能读取流程正文。

## 结果

最终验证全部通过。失败的旧 Run ID `DOC001_validation_20260906_111922` 未复用；失败原因是 Git 默认将中文路径转义，导致校验命令未匹配原始中文路径，并非文档内容错误。最终验证通过 `core.quotepath=false` 获取原始路径后完成。

## 未执行项与残余风险

- 本次只有文档和项目代理规则变化，未执行 C++ 构建、回放或真机测试。
- `AGENTS.md` 的自动发现范围是 `/home/sht/work/DMX_qt` 及其子目录。从其他工作目录开启对话时，应先切换到该项目目录。
- 桌面入口是符号链接；仓库路径被移动或文件被删除时链接会失效。当前链接目标已经验证存在。
- 工作区原有的 6 份专利文档修改未纳入候选提交，也未被覆盖。

## 回滚方法

通过新的 revert PR 回退本任务对应提交，并删除桌面符号链接。禁止重写 `main` 历史。
