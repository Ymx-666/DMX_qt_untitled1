# OPS-001：2026-09-04 DMX 工程基线验证报告

## 1. 结论

- 结论：**工程基线门禁通过，可提交 PR 评审**
- 源码提交：`dc1b789e0f7a3785946bf1bc942585bfc6844fce`
- 分支：`ops/OPS-001-engineering-baseline-20260904`
- Run ID：`OPS001_baseline_20260904_152127`
- 验证日期：2026-09-04

本结论表示当前 DMX 和 DMX_test 候选基线可编译、可回放，且关键单元测试通过。它不等同于算法精度验收、全天稳定性验收或真机验收。

## 2. 候选内容

- DMX 现场设备版和 DMX_test 回放版。
- RGB/BW 360 度全景缓存、环绕裁图和 ROI 放大。
- 前 3 圈全景天空 Mask 建模、几何清理、后台计算与归档。
- weights5 Direct YOLO、传统诊断路径、人工负样本软反馈和事件融合。
- 极坐标目标雷达、目标列表、类别/高度角信息和目标预览。
- 2026-07-23 原始 UDP 日志回放及配套工具。

## 3. 验证环境

| 项目 | 值 |
| --- | --- |
| 操作系统 | Ubuntu 24.04，kernel 7.0.0-30-generic |
| CPU | Intel Core i9-14900K |
| 内存 | 62 GiB |
| GPU | NVIDIA GeForce RTX 5060 Ti |
| GPU 驱动 | 595.84 |
| 主程序 Qt | Qt 5.15.13，`/usr/bin/qmake` |
| 编译器 | g++ 13.3.0 |
| Python | 3.13.9 |
| YOLO provider | CUDAExecutionProvider |

## 4. 版本和校验

| 对象 | SHA-256 |
| --- | --- |
| `dmx_config.json` | `ca164c682828210e9c8532abf991009fa15edebf0da6f8eb0e8132b745c8b648` |
| `build_linux/DMX` | `2177bbac7167c929177a4a69d0552805b99e619fbce3b35231545e615794bde6` |
| `build_linux_test/DMX_test` | `f1281a7a3717474d3a41539cd6081ca9ae6e440ea0260d5446dbc1b953383dc6` |
| weights5 `best.onnx` | `e68c15744109f1ec52952001994dc59bddecbfe6df72c9a2932911fbfe8b094f` |
| 2026-07-23 本地回放 manifest | `56cc7a8bc4495b48aff8070a2720c901a6925268b178e74ffe1467ccb39d7c94` |

模型和数据均位于 Git 仓库外，以上校验值是重现本次结果的必要条件。

## 5. 自动测试

| 门禁 | 结果 |
| --- | --- |
| AppConfig QtTest | 6 passed，0 failed |
| PanoramaCache QtTest | 7 passed，0 failed |
| ManualNegativeStore QtTest | 7 passed，0 failed |
| Python `pytest -q tests` | 37 passed |
| Shell `bash -n` | 通过 |
| Python `compileall` | 通过 |
| `git diff --check` | 通过 |
| 暂存内容凭据扫描 | 通过，未发现口令、Token、API Key 或私钥 |

PanoramaCache 测试明确覆盖了 360 度首尾环绕裁图和“仅允许上下补黑边”行为；ManualNegativeStore 测试覆盖了近邻误检软降权。

## 6. 全量编译

| 产物 | 结果 | 警告 |
| --- | --- | --- |
| DMX | 成功 | 13 |
| DMX_test | 成功 | 13 |

两次均在 `make clean` 后重新编译。警告内容相同，为 2 处 Qt 弃用 API、1 处未使用变量和 10 处未使用函数；无编译错误。这些警告已列为基线技术债，不在 OPS-001 中修改功能代码。

## 7. 标准回放

标准配置通过 `run_dmx_test.sh` 启动，自动执行“设备启动”，运行 45 s；回放源 Run ID 为 `20260904_152326`。

| 指标 | 结果 |
| --- | --- |
| UDP 图像通知速率 | RGB 2.00/s，BW 2.00/s，合计 4.00/s |
| 每个图像 Worker 处理速率 | 平均 2.00 fps |
| 帧处理平均耗时 | 306.1 ms |
| 帧处理最大耗时 | 930 ms，出现在启动/预热阶段 |
| 路径队列 | 132 次采样中 4 次为 1，峰值 1 |
| 读取失败/显式丢帧 | 0 |
| YOLO 帧次数 | 18 |
| YOLO 本体总耗时 | 平均 196.9 ms，P95/最大 259.1 ms |
| YOLO 推理耗时 | 平均 25.1 ms，P95/最大 77.3 ms |
| YOLO 排队延迟 | 平均 43.2 ms，P95/最大 359.8 ms |
| 240 ms 本体预算超时 | 2/18，11.1% |
| 界面检测更新 | 9 次：7 个新事件，2 次 RGB/BW 合并 |

本次回放中，输入和帧处理速率匹配，队列未持续累积。但 YOLO 仍有 2 次超出 240 ms 单帧本体预算，且启动阶段排队延迟峰值达 359.8 ms，后续需用长时回放继续观察 P95/P99。

## 8. 冷启动 Mask 回放

不预加载任何 RGB/BW Mask，以与现场启动逻辑一致的方式运行 42 s。

| 指标 | 结果 |
| --- | --- |
| RGB/BW 建模样本完成圈次 | 第 3 圈 |
| Mask 后台计算 | RGB/BW 均成功，平均 7929 ms，最大 7965 ms |
| Mask 后台归档 | RGB/BW 均成功，平均 317 ms |
| 建模期间 YOLO 帧提交 | 0 |
| Mask ready 后 YOLO 帧提交 | 2 |
| 路径队列 | 123 次采样中 1 次为 1，峰值 1 |
| 读取失败/显式丢帧 | 0 |

日志顺序证明：第 3 圈开始后台计算，Mask 计算完成并进入第 4 圈后才提交 YOLO 帧；计算和归档期间路径队列没有累积。RGB/BW 的 raw、sky、detection Mask 和 JSON 元数据已按时间归档。

## 9. UI 基线证据

- 分辨率：1920 x 1080
- 结果：主界面目标列表、紧凑极坐标雷达和目标放大图均可见，未发现面板互相覆盖。
- 截图：`ui_baseline_fullscreen.png`，位于本次 Run ID 目录。

截图中中部黑色区域为尚未选择 ROI 时的预期状态；本次未进行人工点击、右键误检标记和 ROI 五倍缩放的完整交互回归。

## 10. 证据目录

本次运行证据不提交到 Git，保存在：

```text
/mnt/dmx4t/data/dmx_test/validation/OPS001_baseline_20260904_152127
```

其中包含三组 QtTest 日志、Pytest 日志、两个完整编译日志、标准回放日志、冷启动日志、Mask 归档、校验值和 UI 截图。

原始工作区可校验快照位于：

```text
/mnt/dmx4t/data/project_backups/DMX_qt/20260904_151329
```

## 11. 局限和后续任务

- 标准回放只运行 45 s，冷启动回放只运行 42 s，未覆盖 2026-07-23 全天数据。
- 本次没有接入真实共享盘、相机、转台串口和正交角度回传。
- 目标事件数只证明链路工作，不是带真值标注的准确率、召回率或 mAP 结果。
- 标准 `run_dmx_test.sh` 对传统诊断路径选择 BW，Direct YOLO 仍接收 RGB/BW；冷启动回放明确使用 `BOTH`。
- 需新建性能 Issue 清理 13 条编译警告，并对 YOLO 超预算和启动排队延迟做长时回放。
- 合并前由项目负责人复核删除旧 `项目运行说明.md`、归档旧交接文档和忽略 PDF 的处置决策。
