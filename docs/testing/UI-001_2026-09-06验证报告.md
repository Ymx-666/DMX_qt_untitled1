# UI-001：DMX 正式版界面同步验证报告

## 1. 结论

- 结论：**本地门禁通过，可提交 PR 评审**
- 实现提交：`deb62fcda35ce2398d513b08fad4bcf5ba299dc6`
- 分支：`feature/UI-001-sync-test-ui-to-dmx`
- Run ID：`UI001_validation_20260906_080052`
- 验证日期：2026-09-06

正式 `DMX` 已使用与 `DMX_test` 相同的主界面目标面板、ROI 缩放和高度角显示实现。本任务未修改设备通信、共享盘路径、转台控制或目标检测算法。

## 2. 同步内容

- 主界面底部统一为“目标列表 + 极坐标雷达 + 目标放大图”三栏布局。
- 旧雷达与旧反馈控件在两种构建中均隐藏，避免重复显示或覆盖左上角。
- RGB/BW ROI 弹窗统一以当前视口中心缩放，缩放范围为 0.05 至 5 倍。
- 正式 DMX 使用 26.88 度垂直视场角计算目标高度角，并在目标列表、放大图信息和目标详情页显示。
- 左侧目标项改为两行显示，完整呈现编号、类别、方位、高度角和置信度。

## 3. 自动测试

| 门禁 | 结果 |
| --- | --- |
| Python `pytest` | 37 passed |
| AppConfig QtTest | 6 passed，0 failed |
| PanoramaCache QtTest | 7 passed，0 failed |
| ManualNegativeStore QtTest | 7 passed，0 failed |
| CompactTargetRadarPanel QtTest | 5 passed，0 failed |
| JSON、Shell、Python 编译检查 | 通过 |
| Git 空白错误检查 | 通过 |

新增测试不定义 `DMX_TEST_BUILD`，明确覆盖正式 DMX 的编译路径，并检查三栏几何区域不重叠、目标类别/高度角/置信度、目标预览图和详情页视场角。

## 4. 双程序构建

| 产物 | 结果 | 警告 | SHA-256 |
| --- | --- | --- | --- |
| DMX | 成功 | 13 | `01cc37117e2973c2355afb6be86bc46de1e2e57debc99da73da62308bb34fe2d` |
| DMX_test | 成功 | 13 | `8d78f1929013e5f585e2460ea3bfb79076cf3a835253eff21aa9eda61c2e2600` |

两项均在空构建目录内完成。警告数量和类型与既有工程基线一致，本任务没有引入新的编译警告。

## 5. 界面审查

| 项目 | 结果 |
| --- | --- |
| 正式 DMX 主窗口 | 1600 x 919 截图通过 |
| 三栏组件 | 1320 x 320 截图通过 |
| 旧雷达重复显示 | 未发现 |
| 控件遮挡或越界 | 未发现 |
| 目标列表文本重叠/截断 | 未发现 |
| 26.88 度视场角和高度角 | 正常显示 |

截图文件：

```text
ui/dmx-formal-main.png
fast/compact-panel.png
```

## 6. 回放冒烟测试

| 指标 | 结果 |
| --- | --- |
| 运行窗口 | 45 s |
| RGB/BW 接收摘要 | 45 次 |
| YOLO 结果 | 20 次 |
| 图像读取失败 | 0 |
| 结束后残留测试进程/端口 | 0 |

回放结果证明 UI 同步后 `DMX_test` 仍能接收 2026-07-23 数据并持续更新检测链路。该结果不代表算法精度评估。

## 7. 证据目录

```text
/mnt/dmx4t/data/dmx_test/validation/UI001_validation_20260906_080052
```

`fast`、`build` 和 `replay-smoke` 三套状态均为 `status=passed`、`exit_code=0`。

## 8. 验证边界

- 正式 DMX 截图使用正式构建产物和独立本地端口启动，但未点击设备启动按钮。
- 本次未连接共享盘、相机或转台，也未执行真机目标识别。
- ROI 中心缩放和 5 倍限制已完成代码统一及双程序编译，未使用现场实时 ROI 做人工交互验收。
- GitHub Actions 状态需在 PR 创建后确认；快速测试和 Linux 构建均通过后方可合并。
