# 2026-07-23真实候选记录的视线角模型回算说明

> 数据性质：真实运行产生的候选记录，角度为FOV理想模型回算值，不是经纬仪或标定场测得的真值。

## 1. 输入和完整性

| 项目 | 内容 |
| --- | --- |
| 输入清单 | `/mnt/dmx4t/data/candidates/20260723/16/manifest.jsonl` |
| 输入SHA-256 | `963156e189bc51d58b4726922d425eda7c37a1e37b31b6d97c7828f6e7bf1502` |
| 有效检测记录数 | 387 |
| 输出文件 | `20260723_16时目标视线角模型回算.csv` |
| 输出SHA-256 | `880d31486276f54b910e6f49b2f38d9e362b3426cc199a48b40853ba9491586b` |

## 2. 回算参数

| 参数 | 数值或方法 |
| --- | --- |
| 全景尺寸 | `65536×4096`；纵向4096对应旋转后的线阵有效`Width` |
| 纵向有效视场角 | `26.88°` |
| 主点纵坐标 | 理想值 `2047.5 px` |
| 纵向像素焦距 | 由FOV估算，`8570.060279 px` |
| 高度角模型 | 针孔模型 `atan2(cy-panoY,fy)` |
| 畸变校正 | 未应用 |
| 双通道纵向偏移 | 未应用 |
| 光轴俯仰/设备滚转 | 未应用 |
| 结果等级 | `fov_only_model_estimate` |

线阵原始`Height`是累计扫描行数，承担扫描时间/方位方向，不用于26.88°垂直角换算；本CSV的原始方位仍采用全景横向等角假设，尚未用逐行编码器角重新标定。

## 3. 输出概况

| 指标 | 结果 |
| --- | ---: |
| 原始方位范围 | `33.5303°～350.2661°` |
| 理想相对高度角范围 | `-5.6874°～+13.4052°` |
| 理想相对高度角中位数 | `+1.8813°` |
| 检测框垂直角尺寸范围 | `0.0467°～0.3209°` |

上述范围描述的是该小时候选记录在理想模型下的分布，不代表目标检测精度、测角精度或真实目标高度分布。候选中可能包含误检、固定杂波和重复观察，不能把387条记录解释成387个真实目标。

## 4. 可用于专利材料的内容

- 可以证明当前检测输出已包含足以回算相对视线角的 `panoX、panoY` 和目标框边界字段；
- 可以提供从真实检测记录批量生成方位、高度角和角尺寸的可实施例；
- 可以展示角度字段和标定等级如何随每条检测记录保存；
- 不能用于声称系统已经达到某个绝对测角误差；该结论必须由独立控制点真值试验得到。

## 5. 下一次更新条件

取得RGB/BW各自的 `fy、cy、畸变参数、纵向配准量` 和设备光轴姿态后，应重新生成一份带新标定版本的CSV，保留本文件作为FOV模型基线，不覆盖原结果。两份结果的差值可用于说明标定带来的修正量和技术效果。
