# 附图遮挡与提交检查记录

> 检查日期：2026-09-03
> 检查范围：正式图1～图10、坐标与成像方向核对图、目标视线角获取流程核对图

## 1. 检查结论

12幅SVG均已按原始画布重新渲染并逐张检查，当前未发现以下非预期问题：

- 文字相互覆盖；
- 文字被边框、连线或箭头穿过；
- 模块编号与正文重叠；
- 箭头方向不明或端点被框体遮挡；
- 图名、图号超出画布；
- 中文字体缺失导致方框或乱码；
- 公式、角度符号或下标被裁切。

SVG同时通过XML语法检查。所有图均为黑线白底，不依赖颜色表达必要技术关系。

## 2. 逐图检查表

| 图件 | 遮挡/裁切 | 逻辑连线 | 提交建议 |
| --- | :---: | :---: | --- |
| 坐标与成像方向核对图 | 通过 | 通过 | 已按线阵`Width/Height`语义重绘；仅作工程核对，正式说明书用图9表达核心关系 |
| 目标视线角获取流程核对图 | 通过 | 通过 | 仅作工程和取证操作说明，不编正式图号 |
| 图1 系统总体结构图 | 通过 | 通过 | 可作为正式底稿；已增大输出说明与图号间距 |
| 图2 方法流程图 | 通过 | 通过 | 已调整S105至S107路径，明确“无其他候选时新建” |
| 图3 三类时间关系时序图 | 通过 | 通过 | 三类时间关系的文字和虚线无覆盖 |
| 图4 同源帧互斥示意图 | 通过 | 通过 | 实线表示当前帧分立事件，虚线表示后续帧允许分别关联 |
| 图5 环形方位差示意图 | 通过 | 通过 | 0°/360°两个目标点邻近属于有意表达，不是遮挡 |
| 图6 联合门控及代价选择图 | 通过 | 通过 | 六项证据汇入代价框的连线交汇属于有意表达 |
| 图7 融合事件状态机 | 通过 | 通过 | 已将状态转换条件移到圆框上方，避免压住状态文字 |
| 图8 原始/显示方位分层图 | 通过 | 通过 | 虚线仅表示北向偏置输入和禁止路径 |
| 图9 高度极坐标共同投影图 | 通过 | 通过 | 已注明`Hp=4096`对应线阵Sensor的有效`Width`轴 |
| 图10 实测事件日志示例图 | 通过 | 不适用 | 数据文字较多；建议由代理师决定作为正式图10或转入实施例表格 |

## 3. 本轮版面修正

1. 拉开坐标核对图中全景标题、顶部视线标记和横线之间的距离。
2. 将图1底部输出说明上移，避免与图号视觉粘连。
3. 重排图2的S107、S108文字和编号，并纠正同源帧互斥后的流程走向。
4. 将图7三个状态转换条件移出状态圆框。
5. 重排图9顶部视场文字和边界线，区分已知FOV参数与尚未完成的轴向/姿态标定。
6. 新增目标视线角获取流程核对图，避免把相对角、绝对角和物理高度混为同一结果。
7. 根据海康线阵相机手册，将原图两轴改为“Sensor空间线轴”和“累计扫描行轴”，并明确当前旋转/镜像后分别映射到`panoY`和`panoX`。
8. 重新渲染坐标核对图、测角流程图和图9，确认新增线阵说明未造成文字、边框或箭头遮挡。

## 4. 正式递交前的图件处理

- 当前SVG是可编辑技术底稿。最终文件格式、页边距、字号和图中文字保留范围应按代理师使用的申报模板导出。
- 图1～图9适合保留为说明书附图。图10主要承载实验数据，若代理师认为文字过多，可把数据移入说明书实施例，仅保留简化的三类日志关系图。
- 两幅工程核对图不建议直接编入正式申请，它们用于取得标定材料、核对实施例和向代理师解释技术边界。
- 任何公式或参数更新必须通过生成脚本统一重绘，避免SVG、说明书和权利要求中的数值不一致。
