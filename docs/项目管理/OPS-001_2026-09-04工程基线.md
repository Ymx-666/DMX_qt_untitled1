# OPS-001：建立 2026-09-04 DMX 工程基线

## 任务信息

- 类型：OPS
- 状态：进行中
- 分支：`ops/OPS-001-engineering-baseline-20260904`
- 基准分支：`main`
- 基准提交：`83b3f3b`
- 建立日期：2026-09-04

## 背景

当前 `main` 之上累积了 DMX、DMX_test、检测算法、雷达 UI、负样本反馈、回放工具和项目文档的未提交更改。本任务把用户确认的“当前最新 DMX 与 DMX_test”整理为一个可编译、可测试、可回放和可追溯的候选基线。

## 实施范围

- 对当前工作区做只读快照和 SHA-256 校验。
- 分类源码、测试、文档、运行脚本、构建产物和外部资料。
- 补齐 `.gitignore`，防止构建目录、生成 PDF 和外部检索资料进入提交。
- 运行 C++ 单元测试、Python 测试、DMX/DMX_test 编译和 2026-07-23 固定回放。
- 生成基线验证报告，提交后推送候选分支。

## 非实施范围

- 不修改当前检测阈值、Mask 策略、事件融合和 UI 行为。
- 不将原始数据、模型权重、日志、编译产物和外部专利 PDF 提交到 Git。
- 不直接合并到 `main`，不在未评审前创建基线标签。

## 验收标准

- [x] 从 `main@83b3f3b` 建立独立 OPS 分支。
- [x] 创建并校验源码快照。
- [x] 未检出口令、Token、API Key 或私钥。
- [x] 构建目录和外部/生成 PDF 已排除。
- [ ] 三组 C++ 单元测试全部通过。
- [ ] Python 测试全部通过。
- [ ] DMX 和 DMX_test 全量编译通过。
- [ ] 2026-07-23 固定回放通过，且有 Run ID 和日志路径。
- [ ] `git diff --check` 和提交内容自审通过。
- [ ] 候选分支已推送，等待项目负责人合并。

## 文件处置决策

- `低小慢设备前后端通信接口协议-20260407.docx` 与 `协议文档/` 内文件校验值一致，按目录迁移提交。
- 根目录旧 `项目运行说明.md` 不再代表当前界面和启动方式，本基线保留其已删除状态；当前使用说明由 `latex/DMX说明文档.tex`、`docs/软件使用说明/` 及运行脚本承担。
- 17 个同秒生成的根目录 0 字节算法步骤名文件已移入快照隔离目录，不纳入源码。
- `TASK_HANDOFF.md` 作为历史交接索引保留，其中旧算法状态不用作运行时事实依据。

## 恢复与回滚

- 快照：`/mnt/dmx4t/data/project_backups/DMX_qt/20260904_151329`
- 快照文件数：5924
- 校验文件：`SHA256SUMS`
- 原始已跟踪差异：`tracked_changes.patch`
- 原始工作区状态：`git_status.txt`
- 恢复前必须先在新目录验证快照校验值，不覆盖现有工作区。
