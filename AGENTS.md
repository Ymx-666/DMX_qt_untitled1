# DMX 项目持久开发规则

本文件是 `/home/sht/work/DMX_qt` 的项目级代理规则。任何从本项目目录开始的新对话，在分析或修改 DMX 前都必须先读取本文件、`TASK_HANDOFF.md` 和 `docs/项目管理/DMX每次改动标准流程.md`。

## 1. 适用范围

- 正式程序 `DMX`、回放程序 `DMX_test` 及其共用源码。
- 启动、挂载、配置、协议、模型接入、算法、UI、测试、运维和项目文档。
- 纯问答和只读排查可以不创建 Issue；任何文件修改、系统配置修改、部署或数据清理都必须进入以下流程。

## 2. 强制流程

```text
Issue -> 开发分支 -> 实现与针对性自测 -> 候选 Commit
      -> 完整验证/Run ID -> 报告 Commit -> Push -> PR
      -> CI/人工审核 -> 用户批准合并
      -> 同步 main -> 清理分支 -> 构建/部署 -> 发布记录
```

1. 开始前检查 `git status --short --branch`、当前进程、挂载和任务相关依赖。
2. 不覆盖、暂存、提交或回退用户已有的无关修改；禁止使用 `git reset --hard`、`git checkout --` 清理现场。
3. 为任务创建带验收条件的 GitHub Issue，再从最新 `main` 建短期分支。禁止直接在 `main` 开发和强制推送。
4. 变更保持聚焦。`DMX` 与 `DMX_test` 共用代码时必须同时考虑两个构建模式，不能只验证其中一个。
5. 完成针对性自测后检查 diff，只暂存本任务文件并创建候选 Commit。提交信息使用 Conventional Commits。
6. 在候选 Commit 上按风险执行完整测试并生成唯一 Run ID。算法改动必须使用 2026-07-23 固定回放基准做前后对比；UI 改动必须有 QtTest 或截图检查。
7. 验证报告必须记录候选 Commit 和 Run ID，并作为独立 Commit 提交。PR 必须关联 Issue，列出行为变化、风险、测试结果、配置/模型/端口/路径变化和回滚方法。
8. 等待 GitHub Actions 通过并确认 PR 可合并。除非用户明确授权，代理不得自行合并 PR。
9. 用户确认合并后，快进同步本地 `main`，删除本地和远端任务分支，并重新构建需要发布的程序。
10. 不得中断正在采集或运行的正式 DMX。若构建时旧进程仍在运行，应明确说明重启后新版本才生效。

## 3. 最低测试要求

- 所有代码/配置/脚本修改：`./scripts/test_fast.sh`。
- C++、Qt、构建配置修改：再执行 `./scripts/test_build.sh`。
- 检测、融合、队列、全景、Mask、回放或运行链路修改：再执行 `./scripts/test_replay_smoke.sh`。
- UI 修改：执行相关 QtTest，并检查正式 DMX 和常用窗口尺寸的截图，无重叠、遮挡或越界。
- 设备、挂载、端口、转台修改：先离线验证，再经用户确认安排真机测试。
- 文档修改：执行 `git diff --check`，核对路径、命令、链接和当前实现。

完整选择矩阵、命令、报告模板、发布和回滚步骤见 `docs/项目管理/DMX每次改动标准流程.md`。

## 4. 数据与安全边界

- 原始日志、原始图像、模型权重、运行日志和构建产物不提交 Git，只提交小型 manifest、报告和校验值。
- 2026-07-23 基准数据只读使用，不覆盖或重写。
- 不在代码、文档、日志、Issue、Commit 或 PR 中保存 sudo 密码、访问令牌和共享凭据。
- 数据删除默认只做清单和 `--dry-run`；得到用户明确批准后才能执行。
- 正式运行依赖、端口和路径以 `TASK_HANDOFF.md`、`run_dmx.sh` 和实际生效配置为准，不能凭旧对话记忆猜测。

## 5. 单人项目职责

- 用户：确认需求和验收标准，完成 PR 最终批准，安排真机窗口，决定正式发布或回滚。
- Codex：检查现场，维护 Issue/分支/提交，实施修改，执行自动测试，生成报告和 Run ID，推送 PR，跟踪 CI，并在合并后同步、清理和构建。
- 无第二名开发者时，自动测试、固定回放、截图证据和用户 PR 批准共同构成独立复核，不得因“只有一个人”省略。

## 6. 完成定义

只有同时满足以下条件才能向用户报告任务完成：验收条件满足；必要测试通过；报告与 Run ID 可追溯；PR 已按用户决定处理；合并后本地 `main` 已同步；分支已清理；需要的构建/部署已完成；遗留风险和未执行的真机测试已明确说明。
