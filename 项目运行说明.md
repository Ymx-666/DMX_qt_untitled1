# DMX_qt 项目运行说明

本文档说明项目的运行环境、日志体系、保存全图和录制功能的文件路径与命名规则。

---

## 1. 运行环境

| 项 | Linux（推荐） | Windows |
|---|---|---|
| 操作系统 | Ubuntu 24.04 LTS | Windows 10 |
| Qt 版本 | Qt 5.15+（apt 安装） | Qt 5.4 MSVC2013 x64 |
| 编译器 | g++（apt 安装）或 clang | MSVC2013 (vcvarsall amd64) |
| 构建系统 | qmake + make | qmake + nmake |
| 关键依赖 | FFmpeg、OpenCV4、cifs-utils | 内含 win_setup_test/3rdparty |
| 构建脚本 | `build_linux.sh` | `build_msvc2013.bat` |
| 产出 exe 位置 | `build_linux/untitled1` | `build_msvc2013\release\untitled1.exe` |

### 1.1 Linux 依赖一键安装

```bash
sudo apt install -y \
    qtbase5-dev qtbase5-dev-tools qttools5-dev qttools5-dev-tools \
    qtmultimedia5-dev libqt5multimediawidgets5 \
    libqt5serialport5-dev libqt5svg5-dev \
    libavcodec-dev libavformat-dev libavutil-dev \
    libswscale-dev libavdevice-dev libswresample-dev \
    libopencv-dev pkg-config cifs-utils \
    qtcreator clang
```

### 1.2 设备网络

| 通道 | 端口 | 协议 | 用途 |
|---|---|---|---|
| 图像通知 | 8001 | UDP | 设备发 PATH 包通知客户端有新文件 |
| 命令下发 | 5001 | UDP | 客户端向设备发命令（如 TG_OPEN_DEVICE） |
| 设备应答 | 5002 | UDP | 设备发回的响应 |
| 共享盘 | 445 | SMB v3.0 | 客户端读取设备图像数据 |

默认设备 IP：`192.168.4.1`。客户端 Linux 主机用有线网卡接入 192.168.4.0/24 网段。

---

## 2. 各输出目录速查表

所有目录路径都**支持通过环境变量覆盖**，未设置则用默认值。

| 用途 | 默认路径（Linux） | 默认路径（Windows） | 环境变量 |
|---|---|---|---|
| 保存全图 | `~/dmx_data/saves` | `E:/.trae/program/DMX_qt/untitled1/data/SAVES` | `DMX_SAVE_ROOT` |
| 录制 raw 字节流 | `~/dmx_data/recordings` | `D:/DMX_data` | `DMX_REC_ROOT` |
| 日志文件 | `~/dmx_data/logs` | `E:/.trae/program/DMX_qt/untitled1/data/logs` | `DMX_LOG_ROOT` |
| 共享盘挂载点（Linux）| `/mnt/dmx_share` | — | `DMX_SHARE_MOUNT` |

示例（自定义所有路径）：

```bash
DMX_SAVE_ROOT=/data/dmx/saves \
DMX_REC_ROOT=/data/dmx/recordings \
DMX_LOG_ROOT=/data/dmx/logs \
DMX_SHARE_MOUNT=/mnt/myshare \
    ./build_linux/untitled1
```

---

## 3. 日志系统

### 3.1 双通道：UI 日志框 + 文件日志

| 通道 | 节流 | 用途 |
|---|---|---|
| UI 日志框（侧拉 dock） | 非"重要"类型 15 ms 节流 | 实时可读，不让信息淹没界面 |
| 日志文件（落盘） | **零节流**，全部记录 | 高保真度，事后分析 |

**含义**：UI 看到的可能少几条，但文件里**所有日志都在**。

### 3.2 日志文件保存路径

文件按"日期 + 启动时间"分级归档：

```
<DMX_LOG_ROOT>/                          ← 总目录（默认 ~/dmx_data/logs）
├── 2026-05-27/                          ← 第一层：按日期分目录
│   ├── log_19-03-32.txt                 ← 第二层：当天每次启动一个文件
│   ├── log_19-49-11.txt
│   └── log_21-15-08.txt
├── 2026-05-28/
│   └── log_09-12-44.txt
└── 2026-05-29/
    └── log_14-30-00.txt
```

**文件名规则**：`log_<HH-MM-SS>.txt`，其中时间是程序启动的本地时刻（不是文件最后写时刻）。

**目录创建时机**：程序启动时（MainWindow 构造函数早期）。

**flush 策略**：每条日志立即 flush 落盘——程序崩溃也不会丢日志。

**文件关闭**：程序正常退出时 close（QFile::close 会自动 flush）。

### 3.3 每条日志的行格式

```
[<时间戳>] [<类型>] <消息>
```

例如：

```
[19:49:34.424] [FMT] type=1 decoded=Format_4 size=4096x4096
[19:49:35.299] [FPS] frames=2/s avg=285ms max=337ms
[19:49:37.027] [RX(8001)] pkt=2 ok=1 drop=0 fail=0 rr=0 dg=0 lastdg=0 from=192.168.4.1:48818 last=RGB;/mnt/dmx_share/raw/...
```

| 字段 | 含义 |
|---|---|
| `时间戳` | `HH:MM:SS.mmm`，本地时间，毫秒精度，使用 `qsnprintf` 避免 locale 影响 |
| `类型` | 大类标签（中英文混合），用于过滤与着色 |
| `消息` | 自由文本（包含子字段如 `pkt=2 ok=1`） |

### 3.4 日志类型详解

#### 启动类（每次启动一次性输出）

| 类型 | 含义 |
|---|---|
| `[系统] BUILD exe=...` | 当前运行的 exe 完整路径（自证日志） |
| `[系统] BUILD compiled=...` | 二进制的编译日期 + 时间（`__DATE__ __TIME__`） |
| `[系统] BUILD saveRoot=... recRoot=...` | 当前生效的保存与录制目录 |
| `[系统] BUILD logRoot=... (file=...)` | 当前日志文件位置 |
| `[系统] 指令控制准备就绪，目标: 192.168.4.1` | UDP 5001 命令通道就绪 |
| `[系统] 成功绑定 5002 端口` | UDP 5002 响应通道就绪 |
| `[系统] Worker start PID=...` | VideoWorker 线程启动 |
| `[系统] Bind 8001 ok local=... rcvbuf=...` | UDP 8001 接收通道就绪 |
| `[系统] ImageFormats=...` | Qt 支持的图像格式列表 |
| `[FMT] type=N decoded=Format_X size=WxH` | 首次成功解码后的图像格式诊断（Format 数字对照见 §3.5） |

#### 周期类（每 1 秒输出一次）

| 类型 | 含义 |
|---|---|
| `[RX(8001)]` | UDP 8001 接收统计 |
| `[QUEUE]` | 队列深度监控 |
| `[FPS]` | 处理速率监控 |
| `[SEQ]` | 序号校验汇总（仅当有事件时） |

#### 事件类（触发时输出）

| 类型 | 含义 |
|---|---|
| `[REC]` | 录制状态变化 |
| `[读取失败]` | 共享盘读取失败（带 burst 聚合） |
| `[ROI]` | 用户点击 ROI 区 |
| `[指令下发 (5001)]` | 用户点工具栏命令按钮发出 |
| `[设备应答 (5002)]` | 设备方回应 |
| `[保存全图]` | 保存全图触发/完成 |

#### 关键字段语义

##### `[RX(8001)]` 行字段

```
[RX(8001)] pkt=2 ok=1 drop=0 fail=0 rr=N dg=N lastdg=W from=... last=...
```

| 字段 | 含义 |
|---|---|
| `pkt` | 过去 1 秒该 worker 接收的 PATH 数（含重试，可能虚高） |
| `ok` | 过去 1 秒成功处理并入缓存的帧数 |
| `drop` | 过去 1 秒主动丢弃的包数（应该长期是 0） |
| `fail` | 过去 1 秒读取/解码失败次数 |
| `rr` | 累计 readyRead 信号触发次数（debug 用） |
| `dg` | 累计 datagram 实际读取条数 |
| `lastdg` | 最后一个 datagram 的字节数 |
| `from` | 最后一个 datagram 的来源 IP:port |
| `last` | 最后处理的文件路径（区分 RGB/BW 通道）|

##### `[QUEUE]` 行字段

```
[QUEUE] path=X rec=Y recBytes=Z KB
```

| 字段 | 含义 |
|---|---|
| `path` | 该 worker 的 PathJob 队列当前深度（含正在重试的 m_currentJob） |
| `rec` | RawRecorder 队列当前任务数（仅 RGB worker 报告） |
| `recBytes` | RawRecorder 队列当前持有的总字节数（KB） |

**判断队列健康**：`path > 50` 持续多秒说明该 worker 处理跟不上接收。

##### `[FPS]` 行字段

```
[FPS] frames=N/s avg=Xms max=Yms
```

| 字段 | 含义 |
|---|---|
| `frames` | 过去 1 秒该 worker 成功处理的帧数 |
| `avg` | 过去 1 秒每帧平均处理时间（含等文件就绪 + 读字节 + 解码 + 旋转 + 缩放 + 入缓存） |
| `max` | 过去 1 秒最长一帧的处理时间 |

**估算上限**：`1000 / avg` ≈ 该 worker 理论最大 fps。

##### `[SEQ]` 行字段

```
[SEQ] RGB/BW dup=A reorder=B gap=C maxGap=M maxReorder=R
```

| 字段 | 含义 |
|---|---|
| `dup` | 累计重复包数（设备方发了同样的 fileIdx 多次） |
| `reorder` | 累计真实乱序数（包到达顺序与 fileIdx 不符） |
| `gap` | 累计丢包数（fileIdx 跳过若干） |
| `maxGap` | 单次最大跳号数 |
| `maxReorder` | 单次最大延迟（落后多少个序号） |

##### `[REC]` 事件

| 例 | 含义 |
|---|---|
| `[REC] Start: D:/DMX_data` | 录制开启 |
| `[REC] Roll: D:/DMX_data/REC2_xxx` | 切换到新 session 目录 |
| `[REC] Stop (dropped queue: N jobs, K KB)` | 录制停止；显示停止瞬间还残留在内存队列的任务数与字节数（这部分**不会落盘**） |
| `[REC] Write failed: ...` | 单帧写盘失败 |

##### `[FMT]` 格式诊断（每个 worker 首次解码出现一次）

```
[FMT] type=1 decoded=Format_4 size=4096x4096
```

`Format_N` 数字对照（Qt 5.15）：

| 数字 | 名称 | 字节/像素 | 备注 |
|---|---|---|---|
| 3 | Format_Indexed8 | 1 | 调色板灰度 |
| 4 | Format_RGB32 | 4 | 0xFFRRGGBB |
| 5 | Format_ARGB32 | 4 | 含透明 |
| 13 | Format_RGB888 | 3 | RGB 紧排 |
| 24 | Format_Grayscale8 | 1 | 真灰度（Qt 5.5+） |

---

## 4. 保存全图功能

### 4.1 触发条件

1. 用户点击工具栏 **"保存全图"** 按钮
2. 客户端检查 PanoramaCache 是否已经"满圈"（RGB/BW 两个 stream 的 `validFrames == segments`）
3. 满圈 → 异步保存；未满圈 → 提示"未就绪"

### 4.2 输出目录结构

```
<DMX_SAVE_ROOT>/                                   ← 总目录
└── SAVE2_<epoch_ms>/                              ← 每次保存一个时间戳目录
    ├── rgb/                                       ← 彩色通道，按 tile 分块
    │   ├── rgb_0000.jpg
    │   ├── rgb_0001.jpg
    │   └── ...
    ├── bw/                                        ← 热成像通道，按 tile 分块
    │   ├── bw_0000.jpg                            ← 新版改为 jpg 压缩（旧版是 png）
    │   ├── bw_0001.jpg
    │   └── ...
    ├── raw/                                       ← 仅当 PANO_SAVE_RAW=1
    │   ├── rgb/
    │   │   └── rgb_0000.jpg                       ← 直接从共享盘拷贝的原始字节
    │   └── bw/
    │       └── bw_0000.jpg
    └── manifest.json                              ← 元数据
```

### 4.3 文件命名规则

| 文件 | 命名 |
|---|---|
| Session 目录 | `SAVE2_<epoch_ms>`，其中 epoch_ms 是按钮点击瞬间的毫秒时间戳 |
| 单个 tile | `rgb_NNNN.jpg` / `bw_NNNN.jpg`，其中 NNNN 是 4 位 zero-padded tile 序号 |

**`SAVE2_` 前缀的意义**：这是 ASCII 自证标记。如果你看到目录名是 `〉〇〉」_〇...` 这种乱码，说明跑的是非常老的 exe（没经过 ASCII 命名修复）。

### 4.4 manifest.json 字段

```json
{
  "saveId": 1,
  "createdMs": 1748073600000,
  "panoW": 65536,
  "panoH": 4096,
  "segments": 128,
  "sliceW": 512,
  "rgb": {
    "format": "jpg",
    "quality": 95,
    "tiles": [{"tile":0, "file":"rgb_0000.jpg", "bytes":...}, ...]
  },
  "bw": {
    "format": "jpg",
    "quality": 95,
    "tiles": [{"tile":0, "file":"bw_0000.jpg", "bytes":...}, ...]
  },
  "rawRgb": [{"tile":0, "file":"rgb_0000.jpg", "src":"...", "fileIdx":42, "rxMs":...}, ...],
  "rawBw":  [...]
}
```

### 4.5 PANO_SAVE_RAW 环境变量

| 设置 | 行为 |
|---|---|
| 不设 / `=0` | 只保存重建后的 tile（rgb/、bw/） |
| `=1` | 额外把每个 tile 对应的原始设备文件**字节复制**一份到 raw/ 目录，并在 manifest.json 的 `rawRgb`/`rawBw` 数组里记录每个 tile 的源文件路径、fileIdx、rxMs |

**用途**：需要做事后算法分析时，raw 字节是设备方原始 jpg，未经过任何处理。

---

## 5. 录制功能（Raw 字节流）

### 5.1 触发与控制

| 操作 | 效果 |
|---|---|
| 点工具栏 **"开始录制"**（设备必须先打开）| 进入录制状态，每收到一帧就把字节投递给 RawRecorder |
| 再点一次 **"开始录制"**（按钮 toggle 状态）| 停止录制 |
| 关闭设备 / 关闭程序 | 自动停止录制并 close 当前 session |

### 5.2 输出目录结构

```
<DMX_REC_ROOT>/                                    ← 总目录
├── REC2_<epoch_ms_session_1>/                     ← Session 1
│   ├── rgb/
│   │   ├── 42_1748073601234.jpg                   ← <fileIdx>_<rxMs>.<ext>
│   │   ├── 43_1748073601267.jpg
│   │   └── ...
│   ├── bw/
│   │   ├── 42_1748073601200.jpg
│   │   └── ...
│   └── index.jsonl                                ← 元数据索引
├── REC2_<epoch_ms_session_2>/                     ← Session 2（10 分钟后自动切）
│   └── ...
└── ...
```

### 5.3 文件命名规则

| 项 | 规则 |
|---|---|
| Session 目录 | `REC2_<epoch_ms>`（首帧到达时刻的毫秒时间戳） |
| 子目录 | `rgb/` / `bw/`（按通道分） |
| 单帧 jpg | `<fileIdx>_<rxMs>.<ext>` 例如 `42_1748073601234.jpg` |
| 索引文件 | `index.jsonl` |

字段含义：

- `fileIdx`：从设备端文件名解析出来的递增序号
- `rxMs`：客户端收到该 PATH 通知的毫秒时间戳（**本地时间**，非设备时间）
- `ext`：源文件扩展名（通常 `jpg`），经 ASCII 清洗

**`REC2_` 前缀同样是 ASCII 自证**——与 SAVE2_ 是同源机制。

### 5.4 index.jsonl 格式

每帧一行 JSON（jsonl 格式），追加写入。

```json
{"t":1748073601234,"stream":"RGB","fileIdx":42,"file":"rgb/42_1748073601234.jpg","bytes":8421376,"src":"\\\\192.168.4.1\\data\\rgb_0042.jpg","sender":"192.168.4.1:48818"}
```

| 字段 | 含义 |
|---|---|
| `t` | 客户端接收时刻（毫秒 epoch） |
| `stream` | `RGB` / `BW` |
| `fileIdx` | 设备端原始递增序号 |
| `file` | 在 session 内的相对路径（如 `rgb/42_xxx.jpg`） |
| `bytes` | 实际写入磁盘的字节数 |
| `src` | 设备端原始路径（追溯用） |
| `sender` | UDP 包源（IP:port） |

### 5.5 10 分钟自动分卷

| 触发条件 | 动作 |
|---|---|
| 当前帧的 `rxMs - sessionStartMs >= 10 * 60 * 1000` | 自动关闭当前 session 的 index.jsonl → 创建新 `REC2_<新 epoch_ms>` 目录 → 打开新 index.jsonl |

分卷的好处：
- 单个 index.jsonl 不会无限增长
- 单次崩溃影响范围有限
- 便于按时间段查找

### 5.6 数据持久化保证

| 数据 | 持久化方式 | 异常崩溃时的丢失风险 |
|---|---|---|
| **raw 帧文件**（`.jpg`） | 每帧独立 `open / write / close`，close 触发 OS sync | **几乎不丢**（只有崩溃发生在 close 调用的纳秒级窗口才丢） |
| `index.jsonl` 索引行 | 批量 flush：每 32 条 或 250 ms 触发一次；session 切换 / stop 时强制 flush | 异常崩溃时最多丢失最近 ≤32 条索引；但 raw 帧文件仍在，可后续重建索引 |

**业务约束确认**：raw 字节流是"必须落盘"的核心数据，已做强保证。index.jsonl 只是描述性元数据，可重建。

### 5.7 录制停止时的尾巴

按钮停止录制时，`stopRecording()` 会立即设置 `m_enabled = false` 并清空内存队列。日志会输出：

```
[REC] Stop (dropped queue: N jobs, K KB)
```

- `N=0` → 完全跟上，无任何残留
- `N>0` → 这些帧已经从设备接收但**没来得及写盘就被丢弃**（违反"不丢帧"约束的已知边缘场景）

**最佳实践**：停录制前先等 1-2 秒让队列消化完（看 `[QUEUE] rec=0` 时再停）。

---

## 6. 环境变量速查表

启动前在 shell 设置即可生效。**未设的用默认值**。

```bash
# 所有路径自定义示例
export DMX_SAVE_ROOT=/data/dmx/saves         # 保存全图根
export DMX_REC_ROOT=/data/dmx/recordings     # 录制 raw 根
export DMX_LOG_ROOT=/data/dmx/logs           # 日志文件根
export DMX_SHARE_MOUNT=/mnt/myshare          # 共享盘挂载点（仅 Linux）
export PANO_SAVE_RAW=1                       # 保存全图时额外导出原始字节
./build_linux/untitled1
```

或者一次性传入：

```bash
PANO_SAVE_RAW=1 DMX_SAVE_ROOT=/data/saves ./build_linux/untitled1
```

---

## 7. 启动后的自检流程

启动后立刻看 UI 日志框最上方（淡黄色 4 行 BUILD）：

```
[hh:mm:ss.xxx] [系统] BUILD exe=<完整 exe 路径>
[hh:mm:ss.xxx] [系统] BUILD compiled=<编译日期 + 时间>
[hh:mm:ss.xxx] [系统] BUILD saveRoot=<生效的保存路径> recRoot=<生效的录制路径>
[hh:mm:ss.xxx] [系统] BUILD logRoot=<生效的日志根> (file=<本次日志文件全路径>)
```

| 检查项 | 验证手段 |
|---|---|
| 跑的是新版 exe | `exe=...` 与你刚编译的产出路径一致 |
| 跑的是新代码 | `compiled=...` 时间是你刚才编译的时间 |
| 路径配置正确 | `saveRoot` / `recRoot` / `logRoot` 是你期望的目录 |
| 日志归档生效 | `file=...` 给出本次日志文件的全路径，进文件系统就能直接看到 |

随后约 1 秒内会看到：

```
[系统] 指令控制准备就绪，目标: 192.168.4.1
[系统] 成功绑定 5002 端口...
[系统] Worker start PID=...
[系统] Bind 8001 ok local=0.0.0.0:8001 ...
```

3 秒内（设备如果在推送数据）：

```
[FMT] type=0 decoded=Format_4 size=4096x4096       ← 首帧 RGB 格式诊断
[FMT] type=1 decoded=Format_4 size=4096x4096       ← 首帧 BW 格式诊断
[RX(8001)] pkt=N ...                                ← 每秒接收统计
[FPS] frames=K/s avg=Xms max=Yms                    ← 每秒处理统计
[QUEUE] path=X rec=Y recBytes=Z                     ← 每秒队列深度
```

UI 上方的两个全景图条带应当随着转台转动逐步拼接出图。

---

## 8. 文档版本

| 项 | 值 |
|---|---|
| 文档版本 | 2026-05-27 |
| 适用代码版本 | git commit `19c586c` 及之后 |
| 配套文档 | `README_LINUX.md`（Linux 部署）、`HANDOFF_TO_NEXT_AI.md`（AI 交接说明）、`项目逻辑图_2026-05-24.html`（架构图） |
